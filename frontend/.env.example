# Copy to .env.local for local dev, or set in Netlify's build environment.
# Vite only exposes vars prefixed with VITE_ to the browser.

# Base URL of the Express backend. Defaults to http://localhost:5000 if unset.
VITE_API_URL=http://localhost:5000
